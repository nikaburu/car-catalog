image: microsoft/dotnet:latest

stages:
  - build
  - test

variables:
  project: "/builds/nikaburu/car-catalog"
  project_file: "CarCatalog.csproj"

build:
  stage: build
  variables:
    build_path: "$project/src"
  script:
    - "cd"
    - "dir"
    - "cd $build_path"
    - "dotnet build"

#test:
#  stage: test
#  variables:
#    test_path: "$project/test"
#  script:
#    - "cd $test_path"
#    - "dotnet test"
